import ContactPageClient from "./ContactPageClient";

export const metadata = {
  title: "Contact Us | Digital Code Solution (DCS)",
  description:
    "Get in touch with Digital Code Solution (DCS). Send us a message, book a free consultation, or schedule a call with our software development team today.",
};

export default function ContactPage() {
  return <ContactPageClient />;
}
